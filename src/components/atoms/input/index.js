import React from 'react'
import Box from 'components/primitives/box'

const Input = props => {
    return (
        <Box is="input" border={1} p={1} color="white" bg="grey17" borderColor="grey10" borderRadius={1} />
    )
}

export default Input